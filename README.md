# pg_stat_monitor
pg_stat_monitor es una función de PostgreSQL que combina pg_stat_activity con métricas del sistema operativo para ofrecer una vista detallada en tiempo real de sesiones activas, duración de consultas, uso de CPU, memoria virtual y física, y comandos asociados. Ideal para diagnósticos y monitoreo de rendimiento.
